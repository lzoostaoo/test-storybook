## TestStorybook Design System

To be continued.

## Required packages:

Those packages are required to be installed globally.

- cli-connect-storybook-plugin ( `npm install -g @zeplin/cli-connect-storybook-plugin` )
